<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wenziyue.blog.dal.mapper.ChatSessionMapper">

    <select id="getUserInfo" resultType="com.wenziyue.blog.dal.entity.UserEntity">
        select t1.*
        from TB_WZY_BLOG_CHAT_SESSION_STATUS t0
                 left join TB_WZY_BLOG_USER t1
                           on t0.other_user_id = t1.id
        where t0.user_id = #{userId}
            and t0.session_id in
                <foreach collection="sessionIdList" item="sid" open="(" separator="," close=")">
                    #{sid}
                </foreach>
            and t0.deleted = 0;
    </select>

</mapper>